// src/hooks/useQuizMetadata.ts
// ---------------------------------------------------------------------------
// Legacy hook - now re-exports the React Query version for backward compatibility
// ---------------------------------------------------------------------------

// Re-export the React Query version
export { useQuizMetadata } from './useQuizMetadataQueries'; 